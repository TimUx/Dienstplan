name: Build, Test and Release

on:
  workflow_dispatch:

jobs:
  build-python-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Create production database
      run: |
        New-Item -ItemType Directory -Force -Path data
        python db_init.py data/dienstplan.db
    
    - name: Test Python modules
      run: |
        python -c "import ortools; print('OR-Tools:', ortools.__version__)"
        python -c "import flask; print('Flask:', flask.__version__)"
    
    - name: Get version
      id: version
      run: |
        # Extract version from main project file or use date-based version
        $VERSION = "2.0.${{ github.run_number }}"
        echo "VERSION=$VERSION" >> $env:GITHUB_OUTPUT
        echo "Building version $VERSION"
    
    - name: Build Windows executable with PyInstaller
      run: |
        python -m PyInstaller Dienstplan.spec
    
    - name: Prepare Windows release
      run: |
        New-Item -ItemType Directory -Force -Path release
        Copy-Item dist\Dienstplan.exe release\
        Copy-Item -Recurse data release\
        Copy-Item README.md release\
        Copy-Item LICENSE release\
        echo "Dienstplan v${{ steps.version.outputs.VERSION }}" > release\VERSION.txt
        echo "" >> release\VERSION.txt
        echo "To start the application:" >> release\VERSION.txt
        echo "1. Double-click Dienstplan.exe" >> release\VERSION.txt
        echo "2. The web browser will open automatically" >> release\VERSION.txt
        echo "3. Default URL: http://localhost:5000" >> release\VERSION.txt
        echo "" >> release\VERSION.txt
        echo "Database Location: data\dienstplan.db" >> release\VERSION.txt
        echo "The database is persistent and will retain all your data." >> release\VERSION.txt
        echo "" >> release\VERSION.txt
        echo "Default Login:" >> release\VERSION.txt
        echo "  Email: admin@fritzwinter.de" >> release\VERSION.txt
        echo "  Password: Admin123!" >> release\VERSION.txt
        echo "  IMPORTANT: Change the password after first login!" >> release\VERSION.txt
        echo "" >> release\VERSION.txt
        echo "No Python installation required!" >> release\VERSION.txt
    
    - name: Create Windows ZIP
      run: |
        Compress-Archive -Path release\* -DestinationPath Dienstplan-Windows-v${{ steps.version.outputs.VERSION }}.zip
    
    - name: Upload Windows artifact
      uses: actions/upload-artifact@v4
      with:
        name: windows-executable
        path: Dienstplan-Windows-v${{ steps.version.outputs.VERSION }}.zip

  build-python-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Create production database
      run: |
        mkdir -p data
        python db_init.py data/dienstplan.db
    
    - name: Test Python modules
      run: |
        python -c "import ortools; print('OR-Tools:', ortools.__version__)"
        python -c "import flask; print('Flask:', flask.__version__)"
    
    - name: Get version
      id: version
      run: |
        # Extract version from main project file or use date-based version
        VERSION="2.0.${{ github.run_number }}"
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        echo "Building version $VERSION"
    
    - name: Build Linux executable with PyInstaller
      run: |
        python -m PyInstaller Dienstplan.spec
    
    - name: Prepare Linux release
      run: |
        mkdir -p release
        cp dist/Dienstplan release/
        cp -r data release/
        cp README.md release/
        cp LICENSE release/
        cat > release/VERSION.txt << EOF
        Dienstplan v${{ steps.version.outputs.VERSION }}
        
        To start the application:
        1. Open a terminal in this directory
        2. Run: ./Dienstplan
        3. The web browser will open automatically
        4. Default URL: http://localhost:5000
        
        Database Location: data/dienstplan.db
        The database is persistent and will retain all your data.
        
        Default Login:
          Email: admin@fritzwinter.de
          Password: Admin123!
          IMPORTANT: Change the password after first login!
        
        No Python installation required!
        EOF
        chmod +x release/Dienstplan
    
    - name: Create Linux tar.gz
      run: |
        tar -czf Dienstplan-Linux-v${{ steps.version.outputs.VERSION }}.tar.gz -C release .
    
    - name: Upload Linux artifact
      uses: actions/upload-artifact@v4
      with:
        name: linux-executable
        path: Dienstplan-Linux-v${{ steps.version.outputs.VERSION }}.tar.gz

  create-release:
    needs: [build-python-windows, build-python-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get version
      id: version
      run: |
        VERSION="2.0.${{ github.run_number }}"
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        echo "Building version $VERSION"
    
    - name: Download Windows artifact
      uses: actions/download-artifact@v4
      with:
        name: windows-executable
    
    - name: Download Linux artifact
      uses: actions/download-artifact@v4
      with:
        name: linux-executable
    
    - name: Generate changelog
      id: changelog
      run: |
        echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
        echo "## Changes in this release" >> $GITHUB_OUTPUT
        echo "" >> $GITHUB_OUTPUT
        git log --pretty=format:"- %s (%h)" -10 >> $GITHUB_OUTPUT || echo "- Initial release" >> $GITHUB_OUTPUT
        echo "" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.version.outputs.VERSION }}
        name: Release v${{ steps.version.outputs.VERSION }}
        body: |
          # Dienstplan Release v${{ steps.version.outputs.VERSION }}
          
          **Version 2.0 - Python Edition mit standalone executables für Windows und Linux**
          
          ## 📦 Downloads
          - **Windows**: Dienstplan-Windows-v${{ steps.version.outputs.VERSION }}.zip (keine Python-Installation erforderlich!)
          - **Linux**: Dienstplan-Linux-v${{ steps.version.outputs.VERSION }}.tar.gz (keine Python-Installation erforderlich!)
          
          ## 🚀 Installation & Verwendung
          
          ### Windows (Standalone Executable - Empfohlen!)
          1. Download und entpacken Sie die ZIP-Datei
          2. Doppelklicken Sie auf `Dienstplan.exe`
          3. Das Programm startet automatisch den Webserver und öffnet Ihren Browser
          4. Standard-URL: `http://localhost:5000`
          
          **Keine Installation von Python oder anderen Abhängigkeiten erforderlich!**
          
          ### Linux (Standalone Executable - Empfohlen!)
          1. Download und entpacken Sie die tar.gz-Datei:
             ```bash
             mkdir Dienstplan-Linux
             tar -xzf Dienstplan-Linux-v*.tar.gz -C Dienstplan-Linux
             cd Dienstplan-Linux
             ```
          2. Starten Sie das Programm:
             ```bash
             ./Dienstplan
             ```
          3. Das Programm startet automatisch den Webserver und öffnet Ihren Browser
          4. Standard-URL: `http://localhost:5000`
          
          **Keine Installation von Python oder anderen Abhängigkeiten erforderlich!**
          
          ### Datenbank
          - **Speicherort**: `data/dienstplan.db`
          - **Persistenz**: Die Datenbank ist persistent und behält alle Ihre Daten
          - **Standard-Login**:
            - E-Mail: `admin@fritzwinter.de`
            - Passwort: `Admin123!`
            - ⚠️ **WICHTIG**: Ändern Sie das Passwort nach der ersten Anmeldung!
          
          ### Alternative: Python (für alle Betriebssysteme)
          Falls Sie Python bereits installiert haben oder eine andere Plattform verwenden:
          ```bash
          pip install -r requirements.txt
          python main.py init-db
          python main.py serve
          ```
          
          ## ✨ Features
          - ✅ Standalone Windows Executable (keine Abhängigkeiten)
          - ✅ Standalone Linux Executable (keine Abhängigkeiten)
          - ✅ Produktionsfähige, persistente SQLite-Datenbank
          - ✅ Automatischer Start des Webservers
          - ✅ Automatisches Öffnen des Browsers
          - ✅ Google OR-Tools für optimale Schichtplanung
          - ✅ Responsive Web-UI für Desktop & Mobile
          - ✅ Vollständige Benutzerverwaltung mit Rollen
          
          ${{ steps.changelog.outputs.CHANGELOG }}
        files: |
          Dienstplan-Windows-v${{ steps.version.outputs.VERSION }}.zip
          Dienstplan-Linux-v${{ steps.version.outputs.VERSION }}.tar.gz
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
