# Python OR-Tools Shift Planning System
# Core dependencies for the shift planning migration

# OR-Tools - Constraint Programming solver
ortools>=9.8.3296

# FastAPI - Web framework for REST API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# FastAPI middleware and extensions
bcrypt>=4.0.0
slowapi>=0.1.9
python-multipart>=0.0.22
itsdangerous>=2.2.0

# Standard library enhancements
python-dateutil>=2.8.2

# NumPy version constraint
# OR-Tools and its dependencies (including pandas) now support NumPy 2.x
# Allow NumPy 2.x to avoid conflicts with other packages like opencv
numpy>=1.26.0

# Pandas performance dependencies (indirect dependency via ortools)
# Explicitly specify versions to avoid pandas warnings
numexpr>=2.8.4
bottleneck>=1.3.6

# Database (already using SQLite which is built-in to Python)
# No additional package needed for SQLite3

# Alembic – database migration management (SQLAlchemy is installed as dependency)
alembic>=1.13.0

# PDF export (reportlab) - required for binary builds
reportlab>=4.0.0

# Build tools for creating standalone executable
pyinstaller>=6.0.0

# Excel export (openpyxl) - required for Excel export functionality
openpyxl>=3.1.0

# CSS minification for production builds
csscompressor>=0.9.5
